const d = document;

const card = d.querySelectorAll(".card");
console.log(card);

const formularios = d.querySelectorAll("#formulario");
console.log(formularios);

// si un elemento no existe

const noExiste = d.querySelectorAll(".noExiste");
console.log(noExiste);
