const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponile = true;

// un objeto agrupa todo en una sola variable...

//Object literal
const producto = {
  nombre: "Monitor 20 pulgadas",
  precio: 300,
  disponible: true,
};

console.log(producto);
